const logger = require("../../utils/logger");

// POST request for adding Interviewer
let createInterviewer = async (req, res) => {
  try {
    
  } catch (error) {
    logger.error(error)
  }
};

module.exports = createInterviewer;
